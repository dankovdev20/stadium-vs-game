{
  "name": "stadium-vs-game",
  "private": true,
  "description": "Stadium VS — команды для стенда: setup → build → start (см. STAND.md)",
  "scripts": {
    "setup": "npm ci --prefix client && npm ci --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "stand": "npm run build && npm start",
    "test": "npm test --prefix server && npm run lint --prefix client"
  }
}
