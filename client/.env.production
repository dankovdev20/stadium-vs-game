# Сборка для стенда (npm run build): клиент раздаёт сам сервер, поэтому
# подключаемся к тому же адресу, с которого открыта страница.
# Пустое значение перекрывает VITE_WS_URL из локального .env — IP не вшивается.
VITE_WS_URL=
